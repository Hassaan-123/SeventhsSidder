# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'resturant' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for resturant
  pod 'ThirdSlidder', :git => 'https://github.com/Hassaan-123/ThirdSlidder.git'

  target 'resturantTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'resturantUITests' do
    # Pods for testing
  end

end
